trigger: none
on:
  workflow_dispatch:

schedules:
  - cron: "0 0 0 15 * ? *"
    displayName: cleanup remote git-Branches
    always: true

jobs:
 build:
  steps:
    - bash: sh ./delete_old_branches.sh

